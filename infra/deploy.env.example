# 认证和播放票据密钥必须分别生成，至少 32 字节，不要提交真实值。
JWT_SECRET=
PLAYBACK_TICKET_SECRET=
ADMIN_BOOTSTRAP_PASSWORD=

# 首次使用用户名固定为 admin。登录后在管理后台配置 Emby、ASR、LLM 和 OpenRouter。
# 既有反向代理应使用 HTTPS，并将请求转发到 http://部署服务器IP:18080。
